name: newsapp
description: "News app"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  go_router: ^16.2.0
  flutter_bloc: ^9.1.1
  dio: ^5.9.0
  get_it: ^8.2.0
  flutter_dotenv: ^6.0.0
  sealed_countries: ^2.2.0
  equatable: ^2.0.7
  dropdown_button2: ^2.3.9

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  json_serializable: ^6.9.5
  build_runner: ^2.5.4

flutter:
  uses-material-design: true
  assets:
    - .env
